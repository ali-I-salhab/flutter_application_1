name: Test Production Trigger

on:
  push:
    branches:
      - production

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Say Hello
        run: echo "✅ Workflow triggered on production branch push!"
